const userModel = require("./userModel");
const bookModel = require("./booksModel");

module.exports = {
  userModel,
  bookModel,
};

// this files serves as an index for the Model allowing for easiser imports in other parts of the Application
